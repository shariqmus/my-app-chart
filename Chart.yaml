apiVersion: v2
name: my-app-chart 
description: >
  A generic, reusable Helm chart for deploying any application
  with namespace, deployment, service, and gateway API resources.
type: application
version: 2.1.0
appVersion: "1.0.0"        # version of the "app" this chart deploys

# Optional maintainers
maintainers:
  - name: DevOps Team
    email: devops@platformhub.space
